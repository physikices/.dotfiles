[module/spotify-play]
type = custom/ipc

hook-0 = echo "%{T2}%{T-}"
hook-1 = echo ""
initial = 1

format = <label>
label-padding = 0
format-background = ${colors.bg-alt}
format-foreground = ${colors.fg}
click-left = playerctl play-pause -p spotify
