[module/spotify-next]
type = custom/script

exec = echo " %{T3}%{T-}"
format = <label>
label-padding = 1
format-background = ${colors.bg-alt}
format-foreground = ${colors.fg}
click-left = playerctl next -p spotify
